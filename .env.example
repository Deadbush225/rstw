# Optional override. Leave commented so localhost and LAN pages use their own hostname.
# VITE_SERVER_URL=http://localhost:2567

# Authoritative server. The default host accepts localhost and LAN connections.
SERVER_HOST=0.0.0.0
SERVER_PORT=2567
